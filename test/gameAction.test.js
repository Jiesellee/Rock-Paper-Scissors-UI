// testing for gameAction
// <script src="chai.js" type="text/javascript"></script>var assert = require('assert');
const assert = require('Chai').assert;
const expect = require('Chai').expect;
// const assert = require('assert');
const gameAction = require('../gameAction.js');

//TODO ADD TESTS
/*describe('Array', () => {
  describe('#indexOf()', () => {
    it('should return -1 when the value is not present', () => {
      assert.equal([1, 2, 3].indexOf(4), -1);
    });
  });
});

describe(,()=>{
it('correct selection is displayed'),()=>{
expect(foo).to.equal('You Win! Rock beats Scissors');
expect().to.equal('');
expect().to.equal('');
}); */
